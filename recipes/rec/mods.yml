modules:
  - type: systemd
    system:
      disabled:
        - ModemManager.service
        - systemd-homed.service
        - systemd-machined.service
        - zfs-zed.service
      enabled:
        - displaylink.service